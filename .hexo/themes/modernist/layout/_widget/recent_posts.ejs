<% if (site.posts.length){ %>
<%
  var extraClass = '';
  if (idx.current % 3 === 0) {
    extraClass += ' first';
    if (idx.current >= 3) {
      extraClass += ' newline';
    }
  }
%>
<div class="widget recent-posts<%= extraClass %>">
  <h3 class="title"><%= __('最近文章') %></h3>
  <ul class="entry">
    <% site.posts.sort('date', -1).limit(6).each(function(post){ %>
      <li>
        <a href="<%= config.root %><%= post.path %>"><%= post.title %></a>
      </li>
    <% }); %>
  </ul>
</div>
<% } else {
  idx.current -= 1
} %>
